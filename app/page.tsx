import { Calendar, Card, List, ListItem, WeeklyScheduler } from "@/components";
import Twinzy from "@/components/ui/Members/Twinzy";

const Home = () => {
  const tempSchedules = [];

  return (
    <>
      <WeeklyScheduler className="col-span-12" />
    </>
  );
};

export default Home;
